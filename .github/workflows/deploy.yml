name: Deploy to GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      # --- INSTALL PNPM GLOBALLY (RELIABLE METHOD) ---
      - name: Install pnpm
        run: npm install -g pnpm@9.12.2

      # --- VERIFY PNPM IS WORKING ---
      - name: Verify pnpm
        run: |
          pnpm --version
          echo "pnpm $(pnpm --version) installed"

      # --- INSTALL DEPENDENCIES USING PNPM-LOCK.YAML ---
      - name: Install Dependencies
        run: pnpm install --frozen-lockfile

      # --- BUILD STATIC SITE (Next.js export → ./out) ---
      - name: Build Static Site
        run: pnpm run build

      # --- UPLOAD ./out TO GITHUB PAGES ---
      - name: Upload GitHub Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./out

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4