name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 9

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'pnpm'

      - name: Install dependencies (auto-fix lockfile)
        run: |
          if pnpm install --frozen-lockfile; then
            echo "Using existing pnpm-lock.yaml"
          else
            echo "pnpm-lock.yaml outdated or missing — regenerating..."
            pnpm install --no-frozen-lockfile
          fi

      - name: Export static site
        env:
          GITHUB_PAGES: true
        run: pnpm run export

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./out
          force_orphan: true